# Dataset creation

Questa cartella raccoglie il materiale usato per costruire il subset COCO della fase 0.

- `Fase0_COCO_Locale.ipynb`: notebook principale di preparazione.
- `notebook_outputs/`: CSV, maschere, overlay e file prodotti durante la costruzione del dataset.

Scopo della cartella:

- mostrare come e' stato creato il dataset di lavoro;
- conservare i file intermedi utili a ricostruire il contesto;
- separare la fase di preparazione dati dagli script finali in root.

Nota:

- il notebook e' stato spostato dentro `dataset_creation/`;
- alcuni output gia' salvati nelle celle mostrano ancora il vecchio path locale (`C:\\Users\\loryc\\IdeaProjects\\Test COCO`), ma servono solo come traccia storica dell'esecuzione originale.
